#include <stdio.h>

int main (){

    int temperatura = 17;
    int resultado;

    temperatura > 30 ? printf("Está calor!\n") : printf("Está frio!\n"); 
    // Uso do "?" vem logo depois da condição → significa “o que acontece se for verdadeiro?;
    // Uso do ":" separa a parte do verdadeiro da parte do falso → “senão, o que acontece?;






    return 0;
}